/* global $:false */

'use strict';

$(function () {
  var videoId = $('.aside-video').val();
  var ytURL = 'https://www.googleapis.com/youtube/v3/videos';
  var yt_em = 'https://www.youtube.com/player_api';
  var yt_key = 'AIzaSyDVcGSY5yMaYbz9VePw1t8EFfQHBJNw-jM';
  var urlVideo = ytURL + '?id=' + videoId + '&key='+ yt_key +'&fields=items(snippet(title))&part=snippet';

  if ($('.aside-video').val().length !== 0) {

        $('<iframe/>')
          .addClass('youtube-player video-preview')
          .attr('src', yt_em + videoId + '?autoplay=0&rel=0&showinfo=0')
          .attr('width', '100%')
          .attr('height', '200px')
          .attr('frameborder', '0')
          .appendTo('.youtube-wrapper');

        $('<span/>')
          .addClass('small')
          .text(title)
          .appendTo('.youtube-wrapper');
    
  }
});
